# Core Dependencies
requests>=2.31.0
tqdm>=4.66.0
click>=8.1.0
python-dotenv>=1.0.0
polars>=0.20.0
lxml>=5.0.0
pyarrow>=15.0.0
duckdb>=0.9.0
pydantic>=2.0.0

# Optional Dependencies
# Install specific feature sets as needed:
#
# For NLP features (text processing, sentence splitting, lemmatization):
#   pip install -e ".[nlp]"
#   Includes: spacy, germalemma, unidecode
#
# For text normalization (historical German):
#   pip install -e ".[normalize]"
#   Includes: transformers, torch
#
# For analysis features (entity extraction, topic modeling, emotions):
#   pip install -e ".[analysis]"
#   Includes: openai, anthropic, gliner, bertopic, sentence-transformers
#
# For UI features (Streamlit web interface):
#   pip install -e ".[ui]"
#   Includes: streamlit, plotly, pandas
#
# For everything:
#   pip install -e ".[all]"
#
# For development:
#   pip install -e ".[dev]"
#   Or: pip install -r requirements.txt -r requirements-dev.txt
